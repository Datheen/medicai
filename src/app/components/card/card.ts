import { Component } from '@angular/core';
import { Progress } from "../progress/progress";

@Component({
  selector: 'app-card',
  imports: [Progress],
  templateUrl: './card.html',
  styleUrl: './card.css',
})
export class Card {

}
